# TaskList test - with method call but no call in main

class Counter
  count: Int

  def init
    self.count = 0
  end

  pub def mut increment
    self.count = self.count + 1
  end
end

def main
  puts "Before new"
  let mut c = Counter.new
  puts "After new"
end
