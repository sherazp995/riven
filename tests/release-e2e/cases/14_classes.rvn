class Box
  x: Int

  def init(@x: Int)
  end

  pub def value -> Int
    self.x
  end

  pub def doubled -> Int
    self.x * 2
  end
end

def main
  let b = Box.new(21)
  puts "#{b.value}"
  puts "#{b.doubled}"
end
