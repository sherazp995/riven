def main
  let v = vec![1, 2, 3, 4]
  let doubled = v.iter.map { |n| n * 2 }.to_vec
  let evens = v.iter.filter { |n| n % 2 == 0 }.to_vec
  doubled.each { |n| puts "d #{n}" }
  evens.each { |n| puts "e #{n}" }
end
