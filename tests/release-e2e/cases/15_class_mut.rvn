class Counter
  count: Int

  def init
    self.count = 0
  end

  pub def value -> Int
    self.count
  end

  pub def mut inc
    self.count = self.count + 1
  end
end

def main
  let mut c = Counter.new
  c.inc
  c.inc
  puts "#{c.value}"
end
