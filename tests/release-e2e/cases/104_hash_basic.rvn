def main
  let h = hash!{ "a" => 1, "b" => 2 }
  match h.get("a")
    Some(v) -> puts "a=#{v}"
    None    -> puts "a=missing"
  end
  match h.get("b")
    Some(v) -> puts "b=#{v}"
    None    -> puts "b=missing"
  end
end
