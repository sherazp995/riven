trait Displayable
  def to_display -> String
end

class User
  name: String

  def init(@name: String)
  end

  impl Displayable
    def to_display -> String
      self.name.clone
    end
  end
end

def main
  let u = User.new(String.from("Alice"))
  puts "#{u.to_display}"
end
