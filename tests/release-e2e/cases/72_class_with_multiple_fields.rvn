class Record
  a: Int
  b: Int
  c: Int
  d: Int
  e: Int

  def init(@a: Int, @b: Int, @c: Int, @d: Int, @e: Int)
  end

  pub def sum -> Int
    self.a + self.b + self.c + self.d + self.e
  end
end

def main
  let r = Record.new(1, 2, 3, 4, 5)
  puts "#{r.sum}"
end
