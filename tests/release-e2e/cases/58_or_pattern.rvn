def classify(x: Int) -> String
  match x
    1 | 2 | 3 -> String.from("low")
    4 | 5 | 6 -> String.from("mid")
    _         -> String.from("other")
  end
end

def main
  puts "#{classify(1)}"
  puts "#{classify(5)}"
  puts "#{classify(9)}"
end
