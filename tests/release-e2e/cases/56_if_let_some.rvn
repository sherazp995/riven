def find(n: Int) -> Option[Int]
  if n > 0
    Some(n * 10)
  else
    None
  end
end

def main
  if let Some(n) = find(5)
    puts "#{n}"
  end
  if let Some(n) = find(-1)
    puts "#{n}"
  else
    puts "nothing"
  end
end
