enum MyOpt[T]
  Some(T)
  None
end

def describe(o: MyOpt[Int]) -> String
  match o
    MyOpt.Some(n) -> "some #{n}"
    MyOpt.None    -> String.from("none")
  end
end

def main
  let a = MyOpt.Some(42)
  let b: MyOpt[Int] = MyOpt.None
  puts "#{describe(a)}"
  puts "#{describe(b)}"
end
